Postal Code Automatic v2.0.x is an encapsulated plugin for Zen Cart v2.1 and above that automatically fills state, city and if available street address fields in a form when you enter postal/zip code.
An Ajax request and JavaScript (jQuery) make this possible on the fly without submitting the form.
It uses extra tables with postal codes data per countries.
